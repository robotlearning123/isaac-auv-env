# OceanScale v0.1 — Customer & Investor Brief

**GPU-native underwater robotics simulation. Run docking and control policies faster than real-time on commodity GPU.**

---

## Page 1: The Problem & The Wedge

### The pain (from the AUV operator's desk)

- **Too few hardware test slots.** Sea trials cost $50K per dive day (vessel, crew, weather window). You get 3–5 shots per quarter, and each policy revision needs one.
- **Weeks of policy iteration.** Training a hover or docking policy on CPU-based simulators takes hours per run. Iterating through 20 candidate policies means 2–3 weeks of compute before you can even test in water.
- **The sim-to-real bridge is broken.** Existing underwater simulators (Gazebo+UUV, UWSim) use simplified hydrodynamics. Policies that converge in simulation fail at sea because the physics model doesn't match the real vehicle's drag, added mass, or Coriolis coupling.

### The wedge: 10.5x faster RL iteration

OceanScale runs the full Fossen 6-DOF hydrodynamic model (with cross-coupling damping, added mass, and full Coriolis matrix) on NVIDIA Warp CUDA kernels. The result:

| Metric | Before (PyBullet CPU, n=1) | After (OceanScale GPU, n=64) |
|--------|----------------------------|------------------------------|
| 1M env-steps wall-clock | 603 s | 57 s |
| Throughput (env-steps/s) | 1,661 | 17,427 |

Source: `benchmarks/RESULTS.md`. 10.5× is the measured throughput speedup at n_envs=64 on RTX 5090. Below n=8 PyBullet wins on per-step latency. Policy training time depends on hyperparameters and task complexity, not benchmarked end-to-end here.

### Before / After workflow

**BEFORE (current state):**
- Each policy revision blocks on a sea-trial slot ($50K/dive day, 3–5 per quarter — public industry estimates).
- CPU-based simulators trade fidelity for speed; cross-coupling damping and full Coriolis are usually skipped.

**AFTER (with OceanScale):**
- Pre-sea-trial iteration moves to vectorized GPU sim — 1M env-steps in 57s (measured, RESULTS.md) instead of 603s on PyBullet CPU.
- Fossen 6-DOF + von Benzon 2022 hydrodynamics → fewer fidelity surprises before getting wet.

*Cost numbers above are publicly cited industry estimates. OceanScale reduces the number of pre-sea-trial iterations needed to get a candidate policy worth testing; it does not replace sea trials. Sim-to-real transfer remains an active research problem we have not yet field-validated.*

---

## Page 2: What's Shipping & What's Next

### What's in v0.1 (shipping now, alpha)

| Deliverable | Status |
|-------------|--------|
| `pip install oceanscale` — Python package on PyPI | Done |
| BlueROV2 hover policy (PPO, 1M steps, EV ~0.9, hits depth target to 0.7mm minimum) | Alpha — drifts ~0.41m laterally over a 33s eval; v0.2 work |
| Gym-compatible `ROVEnv` with vectorized envs | Done (150 tests) |
| Colab notebook — zero-install trial | Done |
| OceanScale vs PyBullet benchmark | Done (honest, documented differences) |
| Headless MP4 export (simple + cinematic) | Done |
| Fossen 6-DOF validated against von Benzon 2022 | Done |

Limitations (being honest): hover policy drifts laterally over a full episode (depth-only is solid; full station-keeping is v0.2). No contact/collision, no multi-vehicle, no visual rendering, no sim-to-real transfer validation yet. This is alpha software for design partners, not production.

### Roadmap

**v0.3 (target: Q3 2026)**
- Docking task (approach + terminal guidance)
- Contact handling via Newton rigid-body solver
- Domain randomization for sim-to-real transfer

**v0.5 (target: Q4 2026)**
- Multi-vehicle scenarios (formation, collision avoidance)
- ROS 2 bridge
- Custom vehicle import (URDF → Newton)

**v1.0 (target: 2027)**
- Visual rendering (ray-traced underwater optics)
- Fluid-structure interaction (SPH + rigid body coupling)
- Sim-to-real validated on physical BlueROV2

### Team

Solo founder. PhD-level background in underwater robotics and GPU simulation. Based in Beijing. Currently seeking design partners, not general hiring.

### The ask

Looking for:
1. **Design partners** — AUV operators who will test v0.1 on their vehicle models and provide feedback on physics fidelity and workflow integration
2. **Pre-seed capital** — To hire 1–2 engineers (physics + RL) and accelerate v0.3 docking milestone
3. **Benchmark partners** — Labs with access to physical BlueROV2 hardware for sim-to-real validation studies

---

*Contact: see oceanscale-web.pages.dev for latest information.*

*This brief is for informational purposes. All performance claims cite measured data with documented methodology. "10.5x" refers to throughput at n_envs=64 on BlueROV2 hover, not a general claim about all workloads. OceanScale is alpha software.*
