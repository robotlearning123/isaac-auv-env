# OceanScale Internal Package And Data Artifact Evidence

日期：2026-05-24
范围：内部 alpha readiness；不作为公开发布材料。

## Purpose

本文件验证当前 Python release artifact 实际包含什么，补齐 data / package / pipeline 证据。重点是区分 source tree、sdist、wheel 和 installed wheel 的真实状态。

## Build Evidence

| Item | Evidence | Result |
|---|---|---|
| Build command | `uv build --out-dir /tmp/oceanscale-package-evidence-20260524-1229` | PASS |
| Wheel | `/tmp/oceanscale-package-evidence-20260524-1229/oceanscale-0.1.0a0-py3-none-any.whl` | 535000 bytes |
| Sdist | `/tmp/oceanscale-package-evidence-20260524-1229/oceanscale-0.1.0a0.tar.gz` | 562095 bytes |
| Twine check | `uvx --from twine twine check /tmp/oceanscale-package-evidence-20260524-1229/*` | PASS for wheel and sdist |

Current refreshable artifact: `artifacts/PACKAGE_ARTIFACT_STATE_INTERNAL_2026-05-24.json`, generated by `uv run python scripts/refresh_package_artifact_state.py`. Latest generated status is `package_artifact=green` and `source_data_hygiene=yellow`.

## Source Tree Data State

Current source tree data files:

| Source file | Size | Packaged by pyproject? | Status |
|---|---:|---:|---|
| `oceanscale/data/__init__.py` | 0 bytes | package module | keep |
| `oceanscale/data/bluerov2_station_keep_final.zip` | 521719 bytes | yes via `data/*.zip` | supported artifact |
| `oceanscale/data/vec_normalize.npz` | 1700 bytes | yes via `data/*.npz` | supported artifact |
| `oceanscale/data/vec_normalize.pkl` | 2921 bytes | no | tracked source artifact, cleanup decision needed |

Current package-data declaration in `pyproject.toml`:

```toml
oceanscale = ["data/*.zip", "data/*.npz"]
```

## Automated Package Policy Tests

Added `tests/test_package_data.py` to keep the package-data boundary covered by the normal test suite.

| Test check | Result |
|---|---|
| supported artifacts exist | PASS |
| `data/*.zip` and `data/*.npz` are declared in package-data | PASS |
| `data/*.pkl` is not declared in package-data | PASS |
| wildcard `data/*` is not declared in package-data | PASS |

Focused command: `uv run pytest tests/test_package_data.py -q` -> `4 passed`.

## Wheel Content Evidence

Wheel inspection found:

| Wheel entry | Size |
|---|---:|
| `oceanscale/data/__init__.py` | 0 |
| `oceanscale/data/bluerov2_station_keep_final.zip` | 521719 |
| `oceanscale/data/vec_normalize.npz` | 1700 |
| `oceanscale-0.1.0a0.dist-info/entry_points.txt` | 51 |
| `oceanscale-0.1.0a0.dist-info/METADATA` | 5673 |

Boolean checks:

- `has_zip=True`
- `has_npz=True`
- `has_pkl=False`

## Sdist Content Evidence

Sdist inspection found:

| Sdist entry | Size |
|---|---:|
| `oceanscale-0.1.0a0/oceanscale/data/__init__.py` | 0 |
| `oceanscale-0.1.0a0/oceanscale/data/bluerov2_station_keep_final.zip` | 521719 |
| `oceanscale-0.1.0a0/oceanscale/data/vec_normalize.npz` | 1700 |

Boolean check:

- `has_pkl=False`

## Installed Wheel Smoke

Command sequence:

```bash
python -m venv /tmp/oceanscale-wheel-smoke-20260524-1229
/tmp/oceanscale-wheel-smoke-20260524-1229/bin/python -m pip install --no-deps /tmp/oceanscale-package-evidence-20260524-1229/*.whl
cd /tmp
/tmp/oceanscale-wheel-smoke-20260524-1229/bin/python - <<'PY'
# inspect importlib.metadata and importlib.resources
PY
```

Important detail: the metadata smoke must run outside the repo root. Running from the repo root imports the source tree first and will incorrectly see source-only files such as `vec_normalize.pkl`.

Installed wheel evidence from `/tmp`:

| Check | Result |
|---|---|
| `oceanscale.__file__` | `/tmp/oceanscale-wheel-smoke-20260524-1229/lib/python3.13/site-packages/oceanscale/__init__.py` |
| `oceanscale.__version__` | `0.1.0a0` |
| `importlib.metadata.version('oceanscale')` | `0.1.0a0` |
| console script | `oceanscale -> oceanscale.cli:main` exists |
| installed data entries | `__init__.py`, `__pycache__`, `bluerov2_station_keep_final.zip`, `vec_normalize.npz` |
| installed `has_zip` | `True` |
| installed `has_npz` | `True` |
| installed `has_pkl` | `False` |

## Interpretation

Package artifact boundary is clean: wheel and sdist include the supported `.zip` and `.npz` artifacts and exclude the source-tree `.pkl` file.

Repo data hygiene remains YELLOW because `oceanscale/data/vec_normalize.pkl` is still tracked in source. It is not a package artifact risk for the current wheel, but it remains a source hygiene / ambiguity issue that requires owner approval before deletion.

## Launch Readiness Impact

| Area | Status | Reason |
|---|---|---|
| Local package build | GREEN | wheel and sdist build successfully |
| Twine validation | GREEN | wheel and sdist pass `twine check` |
| Wheel data contents | GREEN | supported zip/npz included; pkl excluded |
| Installed wheel metadata | GREEN | version and console script are present |
| Package data policy tests | GREEN | `tests/test_package_data.py` passes and guards zip/npz vs pkl boundary |
| Source data hygiene | YELLOW | tracked `.pkl` remains in source tree |
| PyPI availability | RED | package is not published on PyPI |
